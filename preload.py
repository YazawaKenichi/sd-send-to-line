import argparse

def preload(parser: argparse.ArgumentParser):
    parser.add_argument(
            "--LINE-Notify-Token",
            help = "[Send to LINE Notify] LINE Notify Token",
            )

